<%= form_with(model: @talk, local: true) do |form| %>
  <div class="talk">
    <%= form.label :talk %>
    <%= form.text_field :talk %>
  </div>
  <%= form.submit %>
<% end %>
<%= link_to'Return to talk list screen', talks_path %>
